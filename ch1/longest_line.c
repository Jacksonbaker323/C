#include "stdio.h"
#define MAXLINE 1000 /* Maximum line size for input */

int get_line(char line[], int maxline);
void copy(char to[], char from[]);


int main() {
  int len;   			/* current line length */
  int max; 			/* maximum line length seen so far */
  char line[MAXLINE]; 		/* initialize a char array with the maximum size as a temporary storage location */
  char longest[MAXLINE];	/* initialize a char array with the max size to hold the longest line */
}
